- const overmatchThreshold = Math.ceil(ownPlating * 14.3);
- let overmatched = false;
ul
	each ship in ships
		if ship.artillery.getCaliber() >= overmatchThreshold					
			- overmatched = true;
			li
				span.ship.enemy #{ship.getLabel()}
				span.caliber
					| #{ship.artillery.getCaliber()}
					span.unit mm
if !overmatched
	.no-overmatch
		| No one in this battle overmatches your armor.

.own-plating
	| Own plating 
	span.info
		| #{ownPlating}
		span.unit mm
	|  is overmatched by guns 
	span.info 
		| #{overmatchThreshold}
		span.unit mm
	|  and larger.


div(style="color: slategray; font-size: 10pt; margin: 100pt 2ch;")
	div(style="font-family: dripicons-v2; font-size: 220%; margin: 0 0.33ch; float: left;") &#xe063;
	| Detection of plating thickness is still very inexact. Take this with a grain of salt.